name: Multi-Platform CI/CD Build

on:
  push:
    branches: [main, master, develop, review-and-enhance-code-for-performance-4b31]
  pull_request:
    branches: [main, master, develop, review-and-enhance-code-for-performance-4b31]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Cache Gradle Dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
            ~/.android/build-cache
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Cache Android NDK
        uses: actions/cache@v4
        with:
          path: |
            /usr/local/lib/android/sdk/ndk
          key: ${{ runner.os }}-ndk-27.0.12077973
          restore-keys: |
            ${{ runner.os }}-ndk-

      - name: Cache Pub Dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - name: Install adependencies
        run: flutter pub get

      - name: Build APK Release
        run: flutter build apk --release --split-per-abi

      - name: Build App Bundle
        run: flutter build appbundle --release

      - name: Rename APK files
        run: |
          BUILD_TIME=$(date +"%Y%m%d_%H%M%S")
          BUILD_NUMBER=${{ github.run_number }}
          COMMIT_SHORT=$(git rev-parse --short HEAD)
          
          cd build/app/outputs/flutter-apk
          
          for apk in *.apk; do
            if [[ $apk == *"arm64-v8a"* ]]; then
              mv "$apk" "app-arm64-${BUILD_TIME}-build${BUILD_NUMBER}-${COMMIT_SHORT}.apk"
            elif [[ $apk == *"armeabi-v7a"* ]]; then
              mv "$apk" "app-arm32-${BUILD_TIME}-build${BUILD_NUMBER}-${COMMIT_SHORT}.apk"
            elif [[ $apk == *"x86_64"* ]]; then
              mv "$apk" "app-x64-${BUILD_TIME}-build${BUILD_NUMBER}-${COMMIT_SHORT}.apk"
            fi
          done
          
          echo "APK files renamed successfully"
          ls -lh

      - name: Upload APK artifacts
        uses: actions/upload-artifact@v4
        with:
          name: app-android-release-build-${{ github.run_number }}
          path: |
            build/app/outputs/flutter-apk/*.apk
            build/app/outputs/bundle/release/app-release.aab
          retention-days: 30

      - name: Send APK to Telegram
        if: success()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          COMMIT_MSG=$(git log -1 --pretty=%B)
          BRANCH_NAME=${GITHUB_REF#refs/heads/}
          BUILD_TIME=$(date +"%Y-%m-%d %H:%M:%S")
          
          cd build/app/outputs/flutter-apk
          
          for apk in *.apk; do
            APK_SIZE=$(du -h "$apk" | cut -f1)
          
            CAPTION="🤖 Android APK Build
          
          File: ${apk}
          Size: ${APK_SIZE}
          Build: #${{ github.run_number }}
          Branch: ${BRANCH_NAME}
          Developer: ${{ github.actor }}
          Commit: ${COMMIT_MSG}
          Time: ${BUILD_TIME}"
          
            curl -F document=@"${apk}" \
                 -F caption="${CAPTION}" \
                 https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendDocument?chat_id=${TELEGRAM_CHAT_ID}
          
            sleep 2
          done

      - name: Send failure notification
        if: failure()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          COMMIT_MSG=$(git log -1 --pretty=%B)
          BRANCH_NAME=${GITHUB_REF#refs/heads/}
          BUILD_TIME=$(date +"%Y-%m-%d %H:%M:%S")
          
          curl -X POST https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage \
               -d chat_id=${TELEGRAM_CHAT_ID} \
               -d parse_mode=HTML \
               -d text="❌ <b>Android Build Failed!</b>
          
          Build: #${{ github.run_number }}
          Branch: ${BRANCH_NAME}
          Developer: ${{ github.actor }}
          Commit: ${COMMIT_MSG}
          Time: ${BUILD_TIME}
          
          <a href='${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}'>View Log</a>"

  build-ios:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Cache Pub Dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - name: Install dependencies
        run: flutter pub get

      - name: Build iOS Release (IPA)
        run: flutter build ipa --release

      - name: Upload iOS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: app-ios-release-build-${{ github.run_number }}
          path: build/ios/ipa/*.ipa
          retention-days: 30

      - name: Send iOS build to Telegram
        if: success()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          COMMIT_MSG=$(git log -1 --pretty=%B)
          BRANCH_NAME=${GITHUB_REF#refs/heads/}
          BUILD_TIME=$(date +"%Y-%m-%d %H:%M:%S")
          
          cd build/ios/ipa
          
          for ipa in *.ipa; do
            IPA_SIZE=$(du -h "$ipa" | cut -f1)
          
            CAPTION="🍎 iOS IPA Build
          
          File: ${ipa}
          Size: ${IPA_SIZE}
          Build: #${{ github.run_number }}
          Branch: ${BRANCH_NAME}
          Developer: ${{ github.actor }}
          Commit: ${COMMIT_MSG}
          Time: ${BUILD_TIME}"
          
            curl -F document=@"${ipa}" \
                 -F caption="${CAPTION}" \
                 https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendDocument?chat_id=${TELEGRAM_CHAT_ID}
          done

      - name: Send failure notification
        if: failure()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          COMMIT_MSG=$(git log -1 --pretty=%B)
          BRANCH_NAME=${GITHUB_REF#refs/heads/}
          BUILD_TIME=$(date +"%Y-%m-%d %H:%M:%S")
          
          curl -X POST https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage \
               -d chat_id=${TELEGRAM_CHAT_ID} \
               -d parse_mode=HTML \
               -d text="❌ <b>iOS Build Failed!</b>
          
          Build: #${{ github.run_number }}
          Branch: ${BRANCH_NAME}
          Developer: ${{ github.actor }}
          Commit: ${COMMIT_MSG}
          Time: ${BUILD_TIME}
          
          <a href='${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}'>View Log</a>"

  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Cache Pub Dependencies
        uses: actions/cache@v4
        with:
          path: |
            ${{ env.LOCALAPPDATA }}\Pub\Cache
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - name: Install dependencies
        run: flutter pub get

      - name: Build Windows Release
        run: flutter build windows --release

      - name: Create Windows Archive
        shell: pwsh
        run: |
          $BUILD_TIME = Get-Date -Format "yyyyMMdd_HHmmss"
          $BUILD_NUMBER = "${{ github.run_number }}"
          $COMMIT_SHORT = git rev-parse --short HEAD
          
          $RELEASE_DIR = "build\windows\x64\runner\Release"
          $ARCHIVE_NAME = "app-windows-${BUILD_TIME}-build${BUILD_NUMBER}-${COMMIT_SHORT}.zip"
          
          Compress-Archive -Path "$RELEASE_DIR\*" -DestinationPath $ARCHIVE_NAME
          
          Write-Output "Windows build archived successfully"
          Write-Output "Archive: $ARCHIVE_NAME"
          Write-Output "Size: $((Get-Item $ARCHIVE_NAME).Length / 1MB) MB"

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: app-windows-release-build-${{ github.run_number }}
          path: app-windows-*.zip
          retention-days: 30

      - name: Send Windows build to Telegram
        if: success()
        shell: pwsh
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          $COMMIT_MSG = git log -1 --pretty=%B
          $BRANCH_NAME = "${{ github.ref_name }}"
          $BUILD_TIME = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
          
          $ARCHIVE = Get-Item app-windows-*.zip | Select-Object -First 1
          $FILE_SIZE = "{0:N2} MB" -f ($ARCHIVE.Length / 1MB)
          
          $CAPTION = @"
          🪟 Windows Build
          
          File: $($ARCHIVE.Name)
          Size: $FILE_SIZE
          Build: #${{ github.run_number }}
          Branch: $BRANCH_NAME
          Developer: ${{ github.actor }}
          Commit: $COMMIT_MSG
          Time: $BUILD_TIME
          "@
          
          $uri = "https://api.telegram.org/bot$env:TELEGRAM_BOT_TOKEN/sendDocument"
          $form = @{
              chat_id = $env:TELEGRAM_CHAT_ID
              caption = $CAPTION
              document = Get-Item $ARCHIVE.FullName
          }
          
          Invoke-RestMethod -Uri $uri -Method Post -Form $form

      - name: Send failure notification
        if: failure()
        shell: pwsh
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          $COMMIT_MSG = git log -1 --pretty=%B
          $BRANCH_NAME = "${{ github.ref_name }}"
          $BUILD_TIME = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
          
          $MESSAGE = @"
          ❌ <b>Windows Build Failed!</b>
          
          Build: #${{ github.run_number }}
          Branch: $BRANCH_NAME
          Developer: ${{ github.actor }}
          Commit: $COMMIT_MSG
          Time: $BUILD_TIME
          
          <a href='${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}'>View Log</a>
          "@
          
          $uri = "https://api.telegram.org/bot$env:TELEGRAM_BOT_TOKEN/sendMessage"
          $body = @{
              chat_id = $env:TELEGRAM_CHAT_ID
              parse_mode = "HTML"
              text = $MESSAGE
          }
          
          Invoke-RestMethod -Uri $uri -Method Post -Body $body

  build-linux:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang cmake ninja-build pkg-config libgtk-3-dev liblzma-dev

      - name: Cache Pub Dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - name: Install dependencies
        run: flutter pub get

      - name: Build Linux Release
        run: flutter build linux --release

      - name: Create Linux Archive
        run: |
          BUILD_TIME=$(date +"%Y%m%d_%H%M%S")
          BUILD_NUMBER=${{ github.run_number }}
          COMMIT_SHORT=$(git rev-parse --short HEAD)
          
          cd build/linux/x64/release/bundle
          ARCHIVE_NAME="app-linux-${BUILD_TIME}-build${BUILD_NUMBER}-${COMMIT_SHORT}.tar.gz"
          
          tar -czf ../../../../../../${ARCHIVE_NAME} .
          
          cd ../../../../../../..
          echo "Linux build archived successfully"
          echo "Archive: ${ARCHIVE_NAME}"
          ls -lh ${ARCHIVE_NAME}

      - name: Upload Linux artifacts
        uses: actions/upload-artifact@v4
        with:
          name: app-linux-release-build-${{ github.run_number }}
          path: app-linux-*.tar.gz
          retention-days: 30

      - name: Send Linux build to Telegram
        if: success()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          COMMIT_MSG=$(git log -1 --pretty=%B)
          BRANCH_NAME=${GITHUB_REF#refs/heads/}
          BUILD_TIME=$(date +"%Y-%m-%d %H:%M:%S")
          
          ARCHIVE=$(ls app-linux-*.tar.gz | head -n 1)
          ARCHIVE_SIZE=$(du -h "$ARCHIVE" | cut -f1)
          
          CAPTION="🐧 Linux Build
          
          File: ${ARCHIVE}
          Size: ${ARCHIVE_SIZE}
          Build: #${{ github.run_number }}
          Branch: ${BRANCH_NAME}
          Developer: ${{ github.actor }}
          Commit: ${COMMIT_MSG}
          Time: ${BUILD_TIME}"
          
          curl -F document=@"${ARCHIVE}" \
               -F caption="${CAPTION}" \
               https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendDocument?chat_id=${TELEGRAM_CHAT_ID}

      - name: Send failure notification
        if: failure()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          COMMIT_MSG=$(git log -1 --pretty=%B)
          BRANCH_NAME=${GITHUB_REF#refs/heads/}
          BUILD_TIME=$(date +"%Y-%m-%d %H:%M:%S")
          
          curl -X POST https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage \
               -d chat_id=${TELEGRAM_CHAT_ID} \
               -d parse_mode=HTML \
               -d text="❌ <b>Linux Build Failed!</b>
          
          Build: #${{ github.run_number }}
          Branch: ${BRANCH_NAME}
          Developer: ${{ github.actor }}
          Commit: ${COMMIT_MSG}
          Time: ${BUILD_TIME}
          
          <a href='${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}'>View Log</a>"

  build-macos:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Cache Pub Dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - name: Install dependencies
        run: flutter pub get

      - name: Build macOS Release
        run: flutter build macos --release

      - name: Create macOS Archive
        run: |
          BUILD_TIME=$(date +"%Y%m%d_%H%M%S")
          BUILD_NUMBER=${{ github.run_number }}
          COMMIT_SHORT=$(git rev-parse --short HEAD)
          
          cd build/macos/Build/Products
          ARCHIVE_NAME="app-macos-${BUILD_TIME}-build${BUILD_NUMBER}-${COMMIT_SHORT}.zip"
          
          zip -r ../../../../../${ARCHIVE_NAME} . -x "*.DS_Store"
          
          cd ../../../../..
          echo "macOS build archived successfully"
          echo "Archive: ${ARCHIVE_NAME}"
          ls -lh ${ARCHIVE_NAME}

      - name: Upload macOS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: app-macos-release-build-${{ github.run_number }}
          path: app-macos-*.zip
          retention-days: 30

      - name: Send macOS build to Telegram
        if: success()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          COMMIT_MSG=$(git log -1 --pretty=%B)
          BRANCH_NAME=${GITHUB_REF#refs/heads/}
          BUILD_TIME=$(date +"%Y-%m-%d %H:%M:%S")
          
          ARCHIVE=$(ls app-macos-*.zip | head -n 1)
          ARCHIVE_SIZE=$(du -h "$ARCHIVE" | cut -f1)
          
          CAPTION="🍎 macOS Build
          
          File: ${ARCHIVE}
          Size: ${ARCHIVE_SIZE}
          Build: #${{ github.run_number }}
          Branch: ${BRANCH_NAME}
          Developer: ${{ github.actor }}
          Commit: ${COMMIT_MSG}
          Time: ${BUILD_TIME}"
          
          curl -F document=@"${ARCHIVE}" \
               -F caption="${CAPTION}" \
               https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendDocument?chat_id=${TELEGRAM_CHAT_ID}

      - name: Send failure notification
        if: failure()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          COMMIT_MSG=$(git log -1 --pretty=%B)
          BRANCH_NAME=${GITHUB_REF#refs/heads/}
          BUILD_TIME=$(date +"%Y-%m-%d %H:%M:%S")
          
          curl -X POST https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage \
               -d chat_id=${TELEGRAM_CHAT_ID} \
               -d parse_mode=HTML \
               -d text="❌ <b>macOS Build Failed!</b>
          
          Build: #${{ github.run_number }}
          Branch: ${BRANCH_NAME}
          Developer: ${{ github.actor }}
          Commit: ${COMMIT_MSG}
          Time: ${BUILD_TIME}
          
          <a href='${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}'>View Log</a>"

  build-web:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Cache Pub Dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - name: Install dependencies
        run: flutter pub get

      - name: Build Web Release
        run: flutter build web --release

      - name: Create Web Archive
        run: |
          BUILD_TIME=$(date +"%Y%m%d_%H%M%S")
          BUILD_NUMBER=${{ github.run_number }}
          COMMIT_SHORT=$(git rev-parse --short HEAD)
          
          cd build/web
          ARCHIVE_NAME="app-web-${BUILD_TIME}-build${BUILD_NUMBER}-${COMMIT_SHORT}.zip"
          
          zip -r ../../${ARCHIVE_NAME} . -x "*.DS_Store"
          
          cd ../..
          echo "Web build archived successfully"
          echo "Archive: ${ARCHIVE_NAME}"
          ls -lh ${ARCHIVE_NAME}

      - name: Upload Web artifacts
        uses: actions/upload-artifact@v4
        with:
          name: app-web-release-build-${{ github.run_number }}
          path: |
            app-web-*.zip
            build/web/
          retention-days: 30

      - name: Deploy to GitHub Pages
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build/web
          cname: false

      - name: Send Web build to Telegram
        if: success()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          COMMIT_MSG=$(git log -1 --pretty=%B)
          BRANCH_NAME=${GITHUB_REF#refs/heads/}
          BUILD_TIME=$(date +"%Y-%m-%d %H:%M:%S")
          
          ARCHIVE=$(ls app-web-*.zip | head -n 1)
          ARCHIVE_SIZE=$(du -h "$ARCHIVE" | cut -f1)
          
          WEB_URL=""
          if [[ "$BRANCH_NAME" == "main" ]] || [[ "$BRANCH_NAME" == "master" ]]; then
            WEB_URL="https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"
          fi
          
          CAPTION="🌐 Web Build
          
          File: ${ARCHIVE}
          Size: ${ARCHIVE_SIZE}
          Build: #${{ github.run_number }}
          Branch: ${BRANCH_NAME}
          Developer: ${{ github.actor }}
          Commit: ${COMMIT_MSG}
          Time: ${BUILD_TIME}
          ${WEB_URL:+🌐 Live URL: ${WEB_URL}}"
          
          curl -F document=@"${ARCHIVE}" \
               -F caption="${CAPTION}" \
               https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendDocument?chat_id=${TELEGRAM_CHAT_ID}

      - name: Send failure notification
        if: failure()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          COMMIT_MSG=$(git log -1 --pretty=%B)
          BRANCH_NAME=${GITHUB_REF#refs/heads/}
          BUILD_TIME=$(date +"%Y-%m-%d %H:%M:%S")
          
          curl -X POST https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage \
               -d chat_id=${TELEGRAM_CHAT_ID} \
               -d parse_mode=HTML \
               -d text="❌ <b>Web Build Failed!</b>
          
          Build: #${{ github.run_number }}
          Branch: ${BRANCH_NAME}
          Developer: ${{ github.actor }}
          Commit: ${COMMIT_MSG}
          Time: ${BUILD_TIME}
          
          <a href='${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}'>View Log</a>"

  build-completed:
    runs-on: ubuntu-latest
    needs: [build-android, build-ios, build-windows, build-linux, build-macos, build-web]
    if: always()

    steps:
      - name: Send completion notification
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          STATUS_EMOJI="✅"
          STATUS_TEXT="All Builds Completed Successfully!"
          
          if [[ "${{ needs.build-android.result }}" != "success" ]] || \
             [[ "${{ needs.build-ios.result }}" != "success" ]] || \
             [[ "${{ needs.build-windows.result }}" != "success" ]] || \
             [[ "${{ needs.build-linux.result }}" != "success" ]] || \
             [[ "${{ needs.build-macos.result }}" != "success" ]] || \
             [[ "${{ needs.build-web.result }}" != "success" ]]; then
            STATUS_EMOJI="⚠️"
            STATUS_TEXT="Some Builds Failed"
          fi
          
          curl -X POST https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage \
               -d chat_id=${TELEGRAM_CHAT_ID} \
               -d parse_mode=HTML \
               -d text="${STATUS_EMOJI} <b>${STATUS_TEXT}</b>
          
          Build: #${{ github.run_number }}
          Platforms: 🤖 Android | 🍎 iOS | 🪟 Windows | 🐧 Linux | 🍎 macOS | 🌐 Web
          Developer: ${{ github.actor }}
          
          📦 Files ready in Artifacts and Telegram
          🔗 <a href='${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}'>View Build Log</a>"
